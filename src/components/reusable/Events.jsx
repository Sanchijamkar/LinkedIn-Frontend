import AddIcon from "../../assets/imgs/add-icon.png";

const Events = () => {
    return (
        <div className="events flex items-center gap-3 mt-3">
            <p>Events</p>
            <img src={AddIcon} alt="Events Add Icon" />
        </div>
    );
}

export default Events;